# Django Settings
# ===============
# Clave secreta de Django. Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# Modo de depuración. Usa 'True' para desarrollo, 'False' para producción
DEBUG=True

# Hosts permitidos. Separa múltiples hosts con comas (ej: localhost,127.0.0.1,example.com)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# ======================
# Opción 1: Usar DATABASE_URL (recomendado para producción)
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# DATABASE_URL=postgresql://user:password@localhost:5432/homecloud

# Opción 2: Usar configuración individual (si no se usa DATABASE_URL)
# Motor de base de datos: 'sqlite3' o 'postgresql'
DB_ENGINE=sqlite3

# Configuración de PostgreSQL (solo necesario si DB_ENGINE=postgresql)
DB_NAME=homecloud
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# Azure Storage Configuration
# ==========================
# Nombre de la cuenta de almacenamiento de Azure->para construir la dirección web (URL)
AZURE_STORAGE_ACCOUNT_NAME=your_storage_account_name

# Clave de acceso de la cuenta de almacenamiento-> La llave secreta que permite leer y escribir archivos.
AZURE_STORAGE_ACCOUNT_KEY=your_storage_account_key

# String de conexión alternativo (puedes usar esto en lugar de account name + key)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account;AccountKey=your_key;EndpointSuffix=core.windows.net

# Nombre del contenedor para almacenar archivos (por defecto: 'files')
AZURE_STORAGE_CONTAINER_NAME=files

# Dominio personalizado opcional para Azure Storage
# Sirve para que los links se vean como archivos.tudominio.com en lugar de azure.microsoft.com....
# AZURE_STORAGE_CUSTOM_DOMAIN=https://your-custom-domain.com